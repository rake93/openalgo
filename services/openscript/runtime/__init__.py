"""Headless server-side execution of compiled OpenScript IR (numpy + openalgo.ta).

The Python `execute_ir` mirrors the browser TypeScript executor
(openalgo-openscript/src/runtime/executor.ts) so server-computed values
match the chart exactly.
"""
