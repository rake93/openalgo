"""OpenAlgo Indicator Engine — server-side OpenScript compilation and headless
execution (architecture doc §15). The `openscript` package is a faithful Python
port of the TypeScript compiler front end shipped in the engine repo; behavioral
equivalence is enforced by the shared conformance fixtures (design decision D3).
"""
